import './Chats.scss'

export const template = ({ }) => (`
.chats
  .chat__contacts
    .chat__contact
  .chat__messages
    .chat__window
      .chat__header
    textarea.chat__message(name='message')
`)
